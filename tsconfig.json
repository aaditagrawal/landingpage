{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"],
  "compilerOptions": {
    // @types/bun for the prebuild scripts; "node" keeps node:fs/node:path resolving.
    "types": ["bun", "node"]
  }
}
